pre-commit:
  parallel: true
  jobs:
    - name: swiftlint
      glob: "*.swift"
      run: mise exec -- swiftlint lint --strict --quiet
    - name: swift-format
      glob: "*.swift"
      run: swift format lint --strict {staged_files}
