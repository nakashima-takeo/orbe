# Orbe

A terminal for *agents* with an **editor** face.

- Open a file with `open_file`
- Save with ⌘S

```swift
let index = LineIndex(text: "a\nb")
```

See [the spec](https://example.com/spec) for details.
